name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build backend artifact
        run: |
          echo "Placeholder for building backend artifact"
          # Example: zip -r backend-artifact.zip ./backend

      - name: Build frontend artifact
        run: |
          echo "Placeholder for building frontend artifact"
          # Example: tar -czvf frontend-artifact.tar.gz -C ./frontend/dist .

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          draft: false
          prerelease: false
